<h4>Listing accounts</h4>

<table>
  <thead>
    <tr>
      <th>Address</th>
      <th>Code</th>
      <th>Date Added</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @accounts.each do |account| %>
      <tr>
        <td><%= account.address %></td>
        <td><%= account.code %></td>
        <td><%= account.created_at.to_formatted_s(:long) %></td>
        <td><%= link_to 'Show', account %></td>
        <td><%= link_to 'Edit', edit_account_path(account) %></td>
        <td><%= link_to 'Destroy', account, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Account', new_account_path, class:'button small radius' %>
